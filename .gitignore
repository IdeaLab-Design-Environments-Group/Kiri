# Dependencies and build output — never committed. Both were tracked until now, which is how
# eight iCloud "name 2.ext" conflict copies got into the repo.
node_modules/
dist/

# macOS junk
.DS_Store

# OCaml build artefacts from the footprint generator; the generated TS is committed, these are not.
ocaml/*.cm*
ocaml/*.o
ocaml/footprints
ocaml/*.tmp.ts

# Agent scratch space.
.scratch/
